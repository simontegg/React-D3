function calculate() { document.getElementById("status").innerHTML=""; document.activeElement.blur();  var vr=document.getElementById("r").value; var vh=document.getElementById("h").value; var res=0;   var count=countInputs(); if(count !=2) { document.getElementById("status").innerHTML="Enter two inputs"; return; }  r=new Number(vr); h=new Number(vh); if( negative(r) || negative(h) ) { complain("inputs cannot be negative"); return; }  if(h>r*2) { complain("Height exceeds diameter"); return; }  var segArea=r*r*Math.acos((r-h)/r) - (r-h)*Math.sqrt(2*r*h-h*h); res=segArea*1; if(isNaN(res)) { complain("Inputs must be numbers"); res=""; return; } document.getElementById("a").innerHTML=round(res); document.activeElement.blur();  } function negative(n) { if(n<0) complain("Negative input"); return (n<0); } function set(id,val) { document.getElementById(id).innerHTML=val; } function complain(s) { document.getElementById("status").innerHTML= s; document.getElementById("a").value=""; } function clear(id) { document.getElementById(id).value=""; } function countInputs() { var count=0; var inputs=document.getElementsByClassName("inp"); for (var i=0; i<2 ; i++ ) { if(inputs[i].value.length>0) count++; } return count; } function keyPress(inputField, e) { if(e.keyCode==13) calculate(); } function clearAll() { document.getElementById("status").innerHTML=""; var inputs=document.getElementsByClassName("inp"); for (var i=0; i<inputs.length ;i++ ) { inputs[i].value=""; } document.getElementById("a").innerHTML="&nbsp;"; } function round(d)  { var multiplier=10000; return Math.round(d*multiplier) / multiplier; };